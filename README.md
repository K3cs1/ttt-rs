# Tic-Tac-Toe Game Rust

1. Build with cargo
    ```
    cargo install cargo-generate
    ```
2. Build with cargo
    ```
    cargo build
    ```
3. Run the application binary
     ```
     cargo run
     ```
